image: ubuntu:20.04

stages:
  - build
  - test
  - deploy

building_server:
  stage: test
  tags:
    - shell
  before_script:
    - sudo cp -r Backend/victory ~/projectvictory-test/
    # - docker build -t server:$CI_COMMIT_SHORT_SHA Backend/victory
  script:
    - ls -l ~/projectvictory-test/
  after_script:
    - docker -version
  only:
    - dev