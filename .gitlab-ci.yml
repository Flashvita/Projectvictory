stages:
  - build
  - test
  - deploy

check-commit:
  stage: test
  tags:
    - docker
  image: ubuntu:20.04
  script:
    - echo "$CI_COMMIT_AUTHOR pushed new commit $CI_COMMIT_SHORT_SHA, message - $CI_COMMIT_MESSAGE"
  only:
    - dev